.venv/
__pycache__/
*.pyc
.idea/
*.log

# ===== 敏感数据，禁止提交 =====
# 含 MySQL 明文账号口令与服务器公网 IP，复制 config.example.json 到 config.json 后自行填写
config/config.json
# 运行时数据：会话代次 .epoch、数据库签名 .last_db、SQLite 兜底库 mes.db、用户密码哈希快照 sync/
data/
# 含学号与 MySQL root 提权命令（明文口令），属私人草稿，不入库
names.txt
