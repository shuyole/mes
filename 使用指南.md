# 用户认证系统使用指南

欢迎使用专为初学者打造的用户认证与权限管理系统！本文档将引导您快速上手系统的使用、功能操作与配置说明。

---

## 1. 运行环境与安装

### 1.1 环境需求
- **操作系统**：Windows 10 / Windows 11
- **Python 环境**：Python 3.10 或更高版本（安装时请勾选 **Add python.exe to PATH**）
- **推荐浏览器**：Google Chrome、Microsoft Edge 或其他现代浏览器

### 1.2 安装依赖
打开项目根目录所在的终端（PowerShell 或 CMD），执行以下命令安装所需依赖库：
```bash
pip install -r requirements.txt
```
依赖清单主要包括：
- `Flask` 与 `Flask-CORS`：构建前后端分离接口
- `requests`：内部服务间通信转发
- `PyMySQL`：支持连接 MySQL 数据库
- `Pillow`：生成登录界面的图形验证码

---

## 2. 一键启动系统

在项目根目录下，直接双击：
```text
start.bat
```
该脚本将依次在后台或独立窗口中拉起三个服务：
1. **6070 数据库代理服务**（`backend/db_server.py`）
2. **8090 后端接口服务**（`backend/api_server.py`）
3. **6031 前端静态托管服务**（`backend/static_server.py`）

随后脚本会自动调用系统默认浏览器打开：
`http://127.0.0.1:6031/login`

---

## 3. 初始账号与功能操作

系统首次启动时会自动在数据库中创建两组默认账号供初学者测试：

| 用户名 | 默认密码 | 角色 | 权限与用途 |
| :--- | :--- | :--- | :--- |
| **admin** | `123456` | 管理员 (`admin`) | 拥有全部管理权限，进入主页与系统设置 |
| **test** | `123456` | 普通用户 (`user`) | 基础用户权限 |

### 3.1 用户登录
1. 打开 `http://127.0.0.1:6031/login`。
2. 输入用户名（如 `admin`）与密码（如 `123456`）。
3. 输入右侧图片中的图形验证码（点击验证码图片可随时更换）。
4. 点击「立即登录」，登录成功后前端将把用户信息保存至 `localStorage`，并自动跳转至系统主页 `http://127.0.0.1:6031/home`。

### 3.2 新用户注册
1. 在登录页点击底部的「没有账号？立即注册」或访问 `http://127.0.0.1:6031/register`。
2. 填写 **用户名**、**用户姓名**、**学号/工号**（重要！用于后续忘记密码找回）、**密码**及**确认密码**。
3. 输入图形验证码后点击「立即注册」。
4. 注册成功后系统将自动跳转回登录页，即可使用新账号登录。

### 3.3 找回密码（学号身份验证）
1. 若忘记密码，在登录页点击「忘记密码？」或访问 `http://127.0.0.1:6031/forgot_password`。
2. 输入您的 **用户名** 以及注册时绑定的 **学号/工号**。
3. 输入您想要重置的 **新密码** 及图形验证码。
4. 点击「重置密码」，后台核对用户名与学号无误后即可将密码更新，完成后即可使用新密码登录。

### 3.4 个人中心与修改密码
1. 登录后进入系统主页 `http://127.0.0.1:6031/home` 或设置页 `http://127.0.0.1:6031/settings`。
2. 页面顶部导航栏展示当前登录人的姓名与角色身份。
3. 点击导航栏的「修改密码」按钮，弹出密码修改窗口：
   - 输入原密码
   - 输入新密码
   - 点击提交，后台核对旧密码正确后即更新为新密码。

### 3.5 退出登录
1. 点击导航栏右上角的「退出」按钮。
2. 前端会自动清理浏览器中的 `localStorage` 登录状态，并重定向返回登录页。

---

## 4. 数据库配置与切换说明

项目支持 **SQLite（本地免配置）** 与 **MySQL（关系型数据库）** 两种引擎。

### 4.1 配置文件路径
系统配置位于 `config/config.json`（若首次运行不存在，系统会自动从 `config/config.example.json` 复制生成）。

```json
{
  "frontend_port": 6031,
  "backend_port": 8090,
  "db_server_port": 6070,
  "database": {
    "type": "sqlite",
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "your_mysql_password",
    "name": "auth_db"
  }
}
```

### 4.2 使用 SQLite（默认推荐，新手最省心）
- 将 `"type"` 设置为 `"sqlite"`。
- 系统将在本地 `data/auth.db` 文件中自动建表并维护数据，无需安装任何数据库软件，开箱即用。

### 4.3 使用 MySQL
- 本地安装并启动 MySQL 服务。
- 将 `config.json` 中的 `"type"` 修改为 `"mysql"`。
- 填写正确的 `user`、`password` 和 `port`。
- 重启服务后，系统会自动创建 `auth_db` 数据库与用户数据表。

---

## 5. 常见问题排查

**Q1：双击 start.bat 提示找不到 Python？**  
请确认电脑已安装 Python 3.10+，并务必在安装界面的第一步勾选了「Add python.exe to PATH」。

**Q2：浏览器打开后提示连接失败或无法访问？**  
请检查黑窗口命令行是否已成功启动，并确认 `6031`、`8090`、`6070` 三个端口没有被其他软件占用。

**Q3：验证码图片显示红叉或加载失败？**  
请在命令行中运行 `pip install pillow`，确保图像处理库安装完整。

**Q4：为什么登录后刷新页面没有被踢回登录页？**  
系统登录状态保存在浏览器的 `localStorage` 中，只要未点击「退出」且未清空浏览器缓存，页面刷新后仍可保持登录状态。
