# 精简版 MES 使用指南

面向工业控制层的生产制造系统教学软件：Web 下单排产、生产数据展示、Python 上位机、无线充电组装产线流程编排。

打开本文件即可按步骤安装、使用和搬到其他电脑。

---

## 1. 需要安装的应用

| 应用 | 是否必须 | 下载与说明 |
|------|----------|------------|
| Windows 10 / 11 | 必须 | 启动脚本按 Windows 编写 |
| Python 3.10 或更高 | **必须** | https://www.python.org/downloads/ 安装时勾选 **Add python.exe to PATH** |
| 浏览器 | 必须 | Edge 或 Chrome，访问系统页面 |
| MySQL 8 | 可选 | https://dev.mysql.com/downloads/mysql/ 没有也能运行 |
| PLC 编程软件 | 不需要 | 产线可本地仿真 |

**不用单独安装：** Flask、PyMySQL、pymodbus。双击 `install.bat` 会按 `requirements.txt` 自动安装。

**也不需要：** Node.js、Java、Redis。

---

## 2. 本机第一次启动

1. 确认已安装 Python，并且勾选了 Add to PATH。
2. 进入本项目文件夹。
3. 双击 **`install.bat`**，等提示「安装完成」。
4. 双击 **`start.bat`**。
5. 浏览器打开：**http://127.0.0.1:6021**

关闭 `start.bat` 那个黑窗口，网站就会停止。

可选：另开窗口双击 `start_plc.bat` 启动虚拟 PLC。占用 502 端口时，Windows 可能要求管理员权限。不启动也能用，产线走本地仿真。

手动安装依赖也可以：

```bat
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt
start "" /D "%CD%\backend" "%CD%\.venv\Scripts\python.exe" db_server.py
start "" /D "%CD%\backend" "%CD%\.venv\Scripts\python.exe" api_server.py
start "" /D "%CD%\backend" "%CD%\.venv\Scripts\python.exe" static_server.py
```

> 三个服务分别是：数据库服务（6060，唯一直接连数据库）、后端接口服务（8080）、前端静态服务（6021）。
> 只启动 `api_server.py` 也可以：它会先探活 6060，未运行就自动把数据库服务拉起来。

---

## 3. 登录与角色

- **管理员**：使用已有管理员账号登录（页面上不会显示账号密码）。
- **其他人**：打开登录页 →「注册普通成员」→ 注册后默认普通成员，账号会写入数据库。

### 管理员可以做什么

- 查看全部页面
- 下单、把工单**下发到产线**
- 强制完工、删除工单
- 上位机启动 / 停止 / 复位 / 急停，写 PLC 寄存器
- 修改 PLC 通讯参数
- 在「系统设置」里管理账号（启用、停用、重置密码、升降级）

### 普通成员可以做什么

- 查看总览、排产、生产数据、上位机监视、产线状态
- 提交工单
- **强制完工、删除工单**
- 不能下发产线、不能控制设备、不能改系统设置、不能管别人账号

---

## 4. 功能模块怎么用

| 菜单 | 对应教学模块 | 做什么 |
|------|----------------|--------|
| 生产总览 | 整体看板 | 看产量、工位状态、最近工单 |
| 下单排产 | 项目1 下单排产、下发生产指令 | 选无线充产品、填数量、生成工单；管理员点「下发产线」 |
| 生产数据 | 项目2 数据展示 | 合格率、趋势图、工位记录 |
| 上位机 | 上位机开发（Python + Modbus） | 看 PLC / 机械臂；管理员可启停和写寄存器 |
| 装配产线 | 项目10–13 无线充电组装产线 | 8 工位流程、PLC 映射、机械臂协同、质量追溯 |
| 系统设置 | 通讯与账号 | 看数据库/PLC 状态；管理员改参数、管用户 |

产线工位顺序：上料 → 线圈装配 → PCB安装 → 外壳合装 → 螺丝锁付 → 功能测试 → 机械臂下料 → 包装入库。

未包含：工业相机视觉算法、运动控制卡轴控。

---

## 5. 搬到其他电脑

1. 拷贝整个项目文件夹。
2. **不要拷 `.venv`**（换电脑会失效）。
3. 新电脑先装 Python 3.10+（勾选 Add to PATH）。
4. 双击 `install.bat`，再双击 `start.bat`。
5. 浏览器打开 http://127.0.0.1:6021

### 数据库

- 新电脑有 MySQL（默认 `root / 123456`）：自动建库 `mes_db`。
- 没有 MySQL：自动用 `data\mes.db`，功能相同。
- MySQL 密码不一样：改 `config\config.json` 里的 `database.password`。

### 账号会不会一起过去

- MySQL：把原来的 `mes_db` 导出再导入，或两台电脑连同一台数据库。
- SQLite：把原来的 `data\mes.db` 一起拷走。
- 只拷代码、不拷库：新电脑是空库，需要重新注册成员。

---

## 6. 配置文件 `config/config.json`

```json
{
  "host": "0.0.0.0",
  "frontend_port": 6021,
  "backend_port": 8080,
  "database": {
    "type": "auto",
    "host": "localhost",
    "user": "root",
    "password": "123456",
    "name": "mes_db"
  },
  "plc": {
    "ip": "127.0.0.1",
    "port": 502
  }
}
```

- `database.type`：`auto`（推荐）/ `mysql` / `sqlite`
- `frontend_port`：前端页面端口，默认 6021（浏览器访问的就是这个）
- `backend_port`：后端接口端口，默认 8080（页面内部调用，无需直接访问）
- `plc.ip`、`plc.port`：真实 PLC 地址；没有 PLC 可保持默认

改完后重新运行 `start.bat`。

---

## 7. 常见问题

**双击 install.bat 提示找不到 Python**  
重新安装 Python，勾选 Add python.exe to PATH，然后重开命令行或重新双击。

**浏览器打不开页面**  
确认 `start.bat` 窗口还在，地址是 http://127.0.0.1:6021 ，端口没被占用。

**提示无法连接 PLC**  
正常。没开虚拟 PLC 或不连真机时，产线仍可仿真。设置页会显示离线。

**502 端口启动失败**  
`start_plc.bat` 以管理员运行，或跳过虚拟 PLC。

**换电脑后原来的工单没了**  
没有把数据库一起拷走。见第 5 节。

---

## 8. 主要文件与目录

| 文件 / 目录 | 作用 |
|------|------|
| `使用指南.md` | 本使用说明 |
| `文件功能说明.md` | 全量文件逐一对应功能详解 |
| `需要的应用.txt` | 应用清单（纯文本） |
| `install.bat` | 一键安装 Python 包 |
| `start.bat` | 一键启动网站（依次启动数据库服务、接口服务、静态服务三个服务） |
| `frontend/index.html` | 前端 SPA 单页核心入口骨架 |
| `frontend/Templates/` | 14 个业务功能的纯静态 HTML 页面片段 |
| `frontend/static/css/` | 17 个独立样式文件（全局主题 + 业务专属样式） |
| `frontend/static/js/` | 20 个独立脚本（底层核心封装 + 页面业务交互） |
| `backend/api_server.py` | 后端接口服务主入口（8080 端口，约 120 行；启动时确保 6060 数据库服务就绪） |
| `backend/db_server.py` | 数据库服务（6060 端口，全项目唯一直接连接数据库的进程，对外提供 SQL 网关） |
| `backend/core/` | 核心驱动层：Modbus TCP 通讯驱动（`plc.py`）与产线状态机仿真引擎（`line_engine.py`） |
| `backend/routes/` | 业务蓝图层：6 大业务蓝图（`auth`, `orders`, `line`, `scada`, `production`, `system`） |
| `backend/static_server.py` | 前端静态托管服务（6021 端口，支持 SPA 路由 fallback） |
| `backend/common.py` | 共享模块：配置加载 / 会话鉴权 / 日志 / 数据库访问客户端（SQL 经 HTTP 转发给 6060） |
| `backend/virtual_plc.py` | 独立 Modbus TCP 虚拟 PLC 模拟器（从站 502 端口） |
| `config/config.json` | 端口 / 数据库 / 数据库服务 / PLC 网络配置 |
| `data/mes.db` | 本地 SQLite 降级数据库文件 |

---

## 9. 前后端分离与模块化架构说明

本项目严格采用**纯前后端分离**与**高内聚低耦合的模块化设计**，便于二次开发与扩展。

### 1. 前后端分离维度

- **三服务独立监听**：
  - **前端静态服务（6021 端口）**：由 `static_server.py` 托管，仅提供纯静态资源（HTML/CSS/JS），所有非文件路由自动 Fallback 到 `index.html`，不连接数据库、不包含业务逻辑。
  - **后端接口服务（8080 端口）**：由 `api_server.py` 托管，为纯 RESTful JSON API 服务，不渲染任何 HTML 模板；进程内不持有数据库连接。
  - **数据库服务（6060 端口）**：由 `db_server.py` 托管，是唯一直接连接数据库的进程；8080 通过 `common.db_cursor()` 把 SQL 经 HTTP 转发给它执行（会话内提交、出错回滚、结束释放连接）。
- **纯客户端渲染（SPA）**：
  - 14 个功能页面全部采用原生 HTML 片段，由前端路由 `router.js` 监听哈希变化动态加载。
  - 页面数据全部由原生 JS 通过 `fetch()` 向 8080 接口异步请求获取，通过 `credentials: 'include'` 携带 Cookie 完成跨域鉴权。
- **可独立部署**：
  - 前端可独立打包部署到 Nginx、CDN 或静态服务器；后端可作为独立微服务部署，前后端仅通过 RESTful API 契约通讯；数据库服务可单独部署在能访问数据库的机器上（改 `config.json` 的 `db_service.host` 指向它）。

### 2. 模块化设计维度

- **后端分层模块化**：
  - **服务入口（`api_server.py`）**：仅负责创建 Flask、配置跨域、挂载蓝图、确保 6060 数据库服务就绪及启动后台守护线程。
  - **数据库服务（`backend/db_server.py`）**：唯一直接连接数据库的进程，负责引擎探测与降级、建库建表、SQL 事务会话与业务表 JSON 快照。
  - **核心驱动层（`backend/core/`）**：
    - `plc.py`：Modbus TCP 硬件通讯驱动、客户端连接池、`PLC_STATE` 缓存与轮询保活线程。
    - `line_engine.py`：产线全局状态机（真实 `LINE` / 虚拟 `VIRTUAL`）、ST01~ST06 工位节拍推进、托盘移载、合格判定与仿真主循环线程。
  - **业务 API 蓝图层（`backend/routes/`）**：
    - `auth.py`：登录、注册、图形验证码生成 (Pillow)、找回密码、会话管理。
    - `orders.py`：工单排产、增删改查、下发投产、手动调进度、示范工单。
    - `line.py`：产线状态、启停急停指令、仿真倍速、工位诊断、故障注入与复位。
    - `scada.py`：PLC 状态监视、ST01 上料气缸/传感器专控、寄存器手写、地址修改。
    - `production.py`：品质追溯、趋势统计、工单/追溯 CSV 报表导出。
    - `system.py`：系统静态配置、心跳探活、操作审计日志、用户管理。
- **前端分层模块化**：
  - **结构层（`frontend/Templates/`）**：14 个独立纯 HTML 片段，按需动态装载。
  - **样式层（`frontend/static/css/`）**：17 个 CSS 样式文件，清晰区分全局设计规范（`app.css`）、HUD 视效（`console.css`）与各业务独立样式。
  - **交互层（`frontend/static/js/`）**：20 个 JS 脚本，严格区分核心底层支撑（`api.js`, `auth.js`, `router.js`, `app.js`, `base.js`）与页面业务逻辑。

