# MES 智能制造执行系统 - 前后端分离与模块化架构说明

> **当前系统已 100% 实现了「前后端分离」与「全模块化」架构！**

本文档详细记录了系统在**前后端分离**与**代码模块化**两个核心技术维度的架构设计、分工对照与全链路测试状态。

---

## 一、前后端分离维度（完全解耦、独立运行）

| 维度 | 当前架构（纯前后端分离） |
| :--- | :--- |
| **服务端口** | **三服务完全分离**：<br>• **前端静态服务（6021 端口）**：只负责提供纯静态 HTML/CSS/JS。<br>• **后端接口服务（8080 端口）**：纯 RESTful API 服务，只返回 JSON 数据，不参与任何页面渲染。<br>• **数据库服务（6060 端口）**：全项目**唯一**直接连接数据库的进程，接口服务把 SQL 以 HTTP 转发给它执行。 |
| **页面渲染** | **前端客户端渲染（SPA）**：<br>所有 14 个页面均为纯 HTML 片段，由前端原生 JS（`router.js`）动态加载，数据全部通过浏览器 `fetch()` 向 8080 接口异步拉取并渲染。 |
| **数据访问** | **数据库驱动只存在于 6060 进程**：<br>6021 与 8080 都不 import 任何数据库驱动、不持有连接；8080 通过 `common.db_cursor()` 向 6060 申请事务会话，逐条执行 SQL 后提交或回滚。 |
| **部署与扩展** | **可独立部署**：前端可部署在 Nginx、CDN 或任意静态托管平台；后端可作为独立的微服务部署在任意服务器，通过 CORS（跨域）完成交互；数据库服务可单独部署在能访问数据库的机器上（`config.json` 的 `db_service.host` 指向它）。 |

---

## 二、模块化维度（清晰分层、各司其职）

系统无论是在**前端**还是**后端**，均已彻底告别“单体大文件”，实现了高内聚、低耦合的模块化设计：

### 1. 后端模块化（从 2102 行单体解耦为专业模块）

```
backend/
├── api_server.py                  # 【服务入口】仅 90 余行，负责启动 Flask、挂载蓝图、确保数据库服务就绪与守护线程
├── db_server.py                   # 【数据库服务】6060 端口，唯一持有数据库驱动的进程：引擎探测/建表/事务会话/SQL 网关
│
├── core/                          # 【核心驱动与状态机层】
│   ├── __init__.py                # 核心驱动与仿真引擎导出
│   ├── plc.py                     # Modbus TCP 通讯驱动、连接池、PLC_STATE 缓存与轮询保活 (约 250 行)
│   └── line_engine.py             # 产线状态机（真实/虚拟）、ST01~ST06 工位推进、节拍与仿真主循环 (约 450 行)
│
└── routes/                        # 【业务 API 蓝图层 (Flask Blueprints)】
    ├── __init__.py                # 蓝图一键注册器 register_routes(app)
    ├── auth.py                    # 登录、注册、图形验证码 (Pillow)、找回密码、会话鉴权
    ├── orders.py                  # 工单排产列表、创建删除、排产下发、手动调进度、示范工单
    ├── line.py                    # 产线状态、启停/急停指令、仿真倍速、工位诊断、故障注入/复位
    ├── scada.py                   # PLC 点位监视、ST01 自动上料气缸/传感器专控、寄存器手写
    ├── production.py              # 品质追溯、趋势统计、工单/追溯 CSV 报表导出
    └── system.py                  # 静态基础配置、心跳探活、操作审计日志、用户管理
```

> `common.py` 是三个服务共用模块：配置加载、业务常量、会话鉴权、权限装饰器，以及把 SQL 转发到 6060 的 `db_cursor()` 入口（详见 [文件功能说明.md](文件功能说明.md)）。

### 2. 数据库服务对外接口（6060）

| 接口 | 方法 | 作用 |
| :--- | :--- | :--- |
| `/db/health` | GET | 探活，返回当前引擎（mysql/sqlite）、主机、库名 |
| `/db/init` | POST | 建库、建表、补列、写入基础数据（幂等） |
| `/db/session/open` | POST | 开启一个事务会话，返回 `session_id` |
| `/db/session/execute` | POST | 在会话中执行 SQL，返回列名与结果行 |
| `/db/session/commit` / `rollback` / `close` | POST | 提交 / 回滚 / 释放会话连接 |

### 3. 前端模块化（按职责划分核心架构与业务逻辑）

```
frontend/
├── index.html                     # SPA 核心宿主外壳（侧边栏、顶部状态栏、Toast、抽屉）
│
├── Templates/                     # 14 个独立页面 HTML 片段（纯静态模板）
│   ├── login.html                 # 登录
│   ├── register.html              # 注册
│   ├── forgot_password.html       # 找回密码
│   ├── home.html                  # 看板大屏
│   ├── line.html                  # 产线追踪
│   ├── virtual.html               # 虚拟仿真
│   ├── orders.html                # 工单排产
│   ├── production.html            # 品质追溯
│   ├── recipes.html               # 工艺配方
│   ├── station_st01.html          # ST01 专控
│   ├── hmi.html                   # 上位机主控
│   ├── alarms.html                # 报警管理
│   ├── settings.html              # 系统设置
│   └── users.html                 # 用户管理
│
└── static/
    ├── css/                       # 17 个独立样式模块（全局设计系统 + 业务专属样式）
    │   ├── app.css                # 全局基础主题变量、按钮、弹窗、抽屉
    │   ├── console.css            # 工业级 HUD 命令中心视效增强
    │   ├── base.css               # 通用工具类
    │   ├── monitor.css            # 看板大屏卡片组件样式
    │   └── (各页面专用 CSS...)
    │
    └── js/                        # 20 个独立功能脚本
        ├── api.js                 # 核心通讯封装（统一处理 Cookie、JSON 与错误）
        ├── auth.js                # 会话鉴权、当前用户状态、角色权限守卫
        ├── router.js              # SPA 客户端哈希路由器与模板动态加载器
        ├── app.js                 # 全局 UI 生命周期（侧边栏、模态框、抽屉、Toast）
        ├── base.js                # 通用格式化与防抖工具
        ├── form.js                # 表单序列化与校验工具
        └── (各业务页面独立 JS...)
```

---

## 三、当前系统状态与全链路验证

1. **一键三服务启动**：
   - 根目录下双击 [start.bat](file:///f:/s/Server/start.bat) 会依次启动数据库服务（6060）、后端 API 服务（8080）与前端静态服务（6021）。
   - 只启动 `api_server.py` 时，它会先探活 6060，未运行则自动拉起数据库服务，避免因漏启动而全部接口报错。
2. **全链路自动化测试**：
   - **API 覆盖**：16 组核心业务场景（鉴权、产线、工单、设备、统计）自动化测试 **100% 通过**；
   - **静态资源**：37 个静态资源（20 个 JS + 17 个 CSS）与 14 个页面 HTML 全部响应 **HTTP 200**；
   - **目录状态**：彻底清除散落的重复 JS 文件与废弃 403 文件，目录结构极其整洁。
