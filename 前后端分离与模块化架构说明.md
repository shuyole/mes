# 前后端分离与模块化架构说明

本项目专为初学者学习与教学演示设计，摒弃了传统模板混编（如 Jinja2/PHP）以及复杂的企业级框架黑魔法，严格采用**三端服务前后端分离架构**与**纯原生 JavaScript 交互**。

---

## 一、三端服务架构设计

系统运行时拆分为三个完全独立的进程服务，各司其职，架构清晰：

```text
┌─────────────────────────┐
│     浏览器 (客户端)      │
│  访问: http://...:6031  │
└────────────┬────────────┘
             │
             ├──────────────────────────────────────────┐
             │ 1. 请求 HTML / CSS / JS 静态文件          │ 2. 发起 Fetch JSON 数据交互
             ▼                                          ▼
┌─────────────────────────┐                ┌─────────────────────────┐
│  前端静态服务 (端口 6031) │                │  后端接口服务 (端口 8090) │
│  (backend/static_server)│                │   (backend/api_server)  │
│  • 纯静态文件代理        │                │  • 提供 RESTful JSON 接口│
│  • 不接触数据库          │                │  • 用户校验 / 注册 / 改密 │
│  • 无任何服务端模板渲染  │                │  • 不直接连数据库        │
└─────────────────────────┘                └────────────┬────────────┘
                                                        │
                                                        │ 3. 内部 HTTP POST 转发 SQL
                                                        ▼
                                           ┌─────────────────────────┐
                                           │  数据库服务 (端口 6070)  │
                                           │   (backend/db_server)   │
                                           │  • 全系统唯一直连数据库   │
                                           │  • 选择 SQLite 或 MySQL │
                                           │  • 执行原生 SQL 并返回JSON│
                                           └────────────┬────────────┘
                                                        │
                                           ┌────────────┴────────────┐
                                           ▼                         ▼
                              ┌──────────────────────┐  ┌──────────────────────┐
                              │  backend/db_sqlite   │  │   backend/db_mysql   │
                              │  (原生 SQLite, ? 占位)│  │ (原生 MySQL, %s 占位)│
                              └──────────────────────┘  └──────────────────────┘
```

### 1. 前端静态托管服务（端口 `6031`）
- **对应源码**：`backend/static_server.py`
- **核心职责**：通过 Flask 专职托管 `frontend/` 目录下的静态资源。
- **特点**：
  - 没有任何数据库引用，也没有任何业务接口。
  - 请求 `/login` 时，直接返回 `frontend/Templates/login.html` 原生页面。
  - 使得前端完全独立，未来即使把前端打包直接扔进 Nginx 或 Apache，也能 100% 正常运行。

### 2. 后端接口服务（端口 `8090`）
- **对应源码**：`backend/api_server.py` 与 `backend/routes/auth.py`
- **核心职责**：纯 RESTful JSON API 服务。
- **特点**：
  - 不渲染任何 HTML。
  - 开启了标准 CORS 跨域支持，允许来自 6031 端口的 JavaScript 异步请求。
  - 所有的请求入参和返回值均为标准的 JSON 格式。
  - 自身不建立数据库驱动连接，所有对数据的查询和写入均通过 `common.py` 调用 6070 数据库代理服务完成。

### 3. 数据库代理服务（端口 `6070`）
- **对应源码**：`backend/db_server.py`
- **核心职责**：全系统唯一能够与数据库底层打交道的进程。
- **特点**：
  - 统一对外暴露 `/query`（查询）和 `/execute`（写入）两个 HTTP 接口。
  - 屏蔽了不同物理数据库驱动的差异，当需要切换数据库引擎时，只需在 6070 服务中修改即可，上层 API 服务完全不需要关心底层用的是 SQLite 还是 MySQL。

---

## 二、为什么彻底移除 Session？

在很多初学者项目中，常常出现对 Flask `session` 的困惑：
- *“Session 到底是存在哪里的？”*
- *“为什么前后端端口不同时（如 6031 访问 8090），Session 就经常丢失或无法识别？”*
- *“为什么每次发 Fetch 请求都要写复杂的 credentials: 'include'？”*

为了彻底消除上述困扰，本项目采用了当下前后端分离最标准、最直接的做法：**零 Session 架构**。

### 1. 纯透明的认证设计
- **登录时**：后端核对用户名和密码成功后，直接在 JSON 响应体中返回当前用户的信息（用户名、显示名称、角色等）。
- **前端保存**：前端 JavaScript（`login.js`）接收到响应后，直接使用浏览器标准的 `localStorage` 保存：
  ```javascript
  localStorage.setItem("user", JSON.stringify(res.user));
  ```
- **鉴权与展示**：页面（如主页 `home.html`、设置页 `settings.html`）加载时，公用的 `shell.js` 直接从 `localStorage` 读取：
  - 若不存在，说明未登录，直接调用 `window.location.href = "/login"` 跳转回登录页；
  - 若存在，直接在页面右上角展示当前用户的名字和身份标签。
- **退出登录**：点击退出时，前端只需一行代码清除本地缓存：
  ```javascript
  localStorage.removeItem("user");
  window.location.href = "/login";
  ```
没有隐式 Cookie，没有服务端的内存或文件 session 依赖，透明直观，初学者一秒就能看懂数据流向。

---

## 三、原生 SQLite 与 MySQL 独立分离

在很多项目中，为了兼容两种数据库，开发者往往喜欢用同一个文件，并在运行期用 `.replace("%s", "?")` 去把 SQL 语句里的占位符换掉。这种“黑魔法”不仅不可靠，而且极易导致 SQL 语法错误，让初学者摸不着头脑。

本项目将两种数据库的实现彻底拆分成两个完全独立的文件：

### 1. `backend/db_sqlite.py`（原生 SQLite 模块）
- 引入 Python 内置的 `sqlite3`。
- SQL 语句内统一使用 SQLite 原生标准的问号占位符 `?`：
  ```python
  cursor.execute("SELECT * FROM users WHERE username = ?", (username,))
  ```
- 数据库连接直接指向本地文件 `data/auth.db`。

### 2. `backend/db_mysql.py`（原生 MySQL 模块）
- 引入 `pymysql`。
- SQL 语句内统一使用 MySQL 原生标准的格式化占位符 `%s`：
  ```python
  cursor.execute("SELECT * FROM users WHERE username = %s", (username,))
  ```
- 自动负责创建数据库（`CREATE DATABASE IF NOT EXISTS auth_db`）与建表。

### 3. 在 `db_server.py` 中直接分支调用
根据 `config.json` 中的 `type` 字段，简单明确地选择使用哪个文件：
```python
if DB_TYPE == "mysql":
    import db_mysql as db
else:
    import db_sqlite as db
```
代码干净利落，没有一句多余的字符串替换黑魔法。

---

## 四、经典业务交互时序（以登录为例）

```text
[浏览器]                          [6031 静态服务]     [8090 接口服务]     [6070 数据库服务]
   |                                    |                  |                  |
   | 1. GET /login                      |                  |                  |
   |----------------------------------->|                  |                  |
   | 2. 返回 login.html                 |                  |                  |
   |<-----------------------------------|                  |                  |
   |                                    |                  |                  |
   | 3. 用户输入表单并点击登录           |                  |                  |
   | 4. POST /api/login (带JSON数据)    |                  |                  |
   |------------------------------------------------------>|                  |
   |                                    |                  | 5. POST /query   |
   |                                    |                  | (查用户与密码)   |
   |                                    |                  |----------------->|
   |                                    |                  | 6. 返回查询结果  |
   |                                    |                  |<-----------------|
   |                                    |                  |                  |
   | 7. 返回 {"ok": true, "user": ...}  |                  |                  |
   |<------------------------------------------------------|                  |
   |                                    |                  |                  |
   | 8. 前端执行 localStorage.setItem   |                  |                  |
   | 9. window.location.href = '/home'  |                  |                  |
```
