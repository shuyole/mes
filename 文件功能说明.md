# MES 智能制造执行系统 - 文件结构与功能说明

本文档对本项目（工业柔性装配线 MES）中的**每一个文件与目录**进行全面整理与功能说明，方便查阅与维护。

---

## 目录索引
1. [项目整体架构](#一项目整体架构)
2. [根目录文件](#二根目录文件)
3. [后端服务 (`backend/`)](#三后端服务-backend)
4. [前端入口与页面 (`frontend/pages/`)](#四前端入口与页面-frontend)
5. [前端样式表 (`frontend/static/css/`)](#五前端样式表-frontendstaticcss)
6. [前端交互脚本 (`frontend/static/js/`)](#六前端交互脚本-frontendstaticjs)
7. [配置文件与数据目录 (`config/` & `data/`)](#七配置文件与数据目录-config--data)
8. [自动化测试与文档目录 (`tests/` & `代码讲解/`)](#八自动化测试与文档目录)

---

## 一、项目整体架构

- **架构模式**：纯前后端分离单页应用（SPA, Single Page Application）。
- **前端技术栈**：HTML5 + CSS3 + 原生 JavaScript（Vanilla JS，零第三方打包工具，开箱即用）。
- **静态资源服务**：由 [backend/static_server.py](file:///f:/s/Server/backend/static_server.py) 托管，默认端口 `6021`。
- **后端接口服务**：由 [backend/api_server.py](file:///f:/s/Server/backend/api_server.py) 托管，默认端口 `8080`，提供全部 `/api/*` RESTful JSON 接口。
- **数据库支持**：MySQL 与 SQLite 双模自动降级，优先连接远程/本地 MySQL，无法连接时自动无缝切换到本地 SQLite。

---

## 二、根目录文件

| 文件名 | 功能说明 |
| :--- | :--- |
| [start.bat](file:///f:/s/Server/start.bat) | **一键启动批处理脚本**。双击即可在独立控制台窗口中同时启动后端 API 服务（8080）与前端静态服务（6021），并提供常用快捷访问提示。 |
| [README.md](file:///f:/s/Server/README.md) | **项目快速上手说明**。包含项目定位、环境要求、快速启动命令、前后端端口划分及默认账户说明。 |
| [使用指南.md](file:///f:/s/Server/使用指南.md) | **系统详细使用手册**。包含系统部署、运行原理、各功能模块操作指引、账号权限（管理员/普通操作员）、故障排查与测试验证指南。 |
| [names.txt](file:///f:/s/Server/names.txt) | **仿真人名数据池**。用于产线模拟运行时动态生成操作工、质检员的模拟工号与姓名。 |
| [需要的应用.txt](file:///f:/s/Server/需要的应用.txt) | **运行环境与依赖清单**。记录项目所需的 Python 运行时环境及关键第三方库（Flask, Flask-CORS, PyMySQL, pymodbus, Pillow 等）。 |
| `.gitignore` | **Git 忽略配置文件**。排除本地运行产生的缓存文件（`__pycache__`）、临时数据、日志及 IDE 配置（`.idea`、`.trae`）。 |

---

## 三、后端服务 (`backend/`)

后端已完成**模块化解耦重构**，将原 2102 行单体代码拆分为核心驱动层（`core/`）与业务蓝图层（`routes/`），主入口 [backend/api_server.py](file:///f:/s/Server/backend/api_server.py) 精简至 75 行：

### 1. 服务入口与公共层
| 文件名 | 功能说明 |
| :--- | :--- |
| [backend/api_server.py](file:///f:/s/Server/backend/api_server.py) | **后端核心接口服务主入口（8080 端口，约 75 行）**。<br>• 创建 Flask 应用实例与 CORS 跨域配置。<br>• 一键挂载 `routes/` 下全部 6 个业务蓝图。<br>• 启动 `core/` 下的 PLC 通讯轮询与产线仿真守护线程。<br>• 统一异常捕获与启动自愈机制。 |
| [backend/common.py](file:///f:/s/Server/backend/common.py) | **核心公共业务与数据访问层**。<br>• 读取与持久化 [config/config.json](file:///f:/s/Server/config/config.json)。<br>• 数据库连接池与游标上下文（`db_cursor`），负责 MySQL 与 SQLite 双模式自动探测与无缝降级。<br>• 数据库表结构初始化、密码哈希、Session 会话安全令牌校验、权限装饰器、系统操作日志记录。 |
| [backend/static_server.py](file:///f:/s/Server/backend/static_server.py) | **前端静态文件服务器（6021 端口）**。<br>• 托管 [frontend/](file:///f:/s/Server/frontend) 静态文件（HTML/CSS/JS）。<br>• 配置 SPA 路由 Fallback：非文件路径统一回退到 `index.html`。 |
| [backend/virtual_plc.py](file:///f:/s/Server/backend/virtual_plc.py) | **独立 Modbus TCP 虚拟 PLC 模拟器**（从站 502 端口，脱机调试）。 |
| [backend/virtual_plc_manager.py](file:///f:/s/Server/backend/virtual_plc_manager.py) | **虚拟 PLC 进程管理器**（子进程启动、停止、探测与保活）。 |
| [backend/test.py](file:///f:/s/Server/backend/test.py) | **数据库联通性排查脚本**。 |

### 2. 核心驱动与仿真引擎 (`backend/core/`)
| 文件名 | 功能说明 |
| :--- | :--- |
| [backend/core/plc.py](file:///f:/s/Server/backend/core/plc.py) | **Modbus TCP 硬件通讯驱动层**。<br>• 维护与 PLC 的 Modbus TCP 客户端连接池与互斥锁（`plc_lock`）。<br>• 保持全局 `PLC_STATE` 状态快照与断线重连冷却机制。<br>• 提供寄存器批量读写（`read_from_plc`, `write_to_plc`, `write_plc_map`）。<br>• 常驻后台 `poll_plc_status` 轮询保活线程。 |
| [backend/core/line_engine.py](file:///f:/s/Server/backend/core/line_engine.py) | **工业流水线推进与仿真状态机**。<br>• 维护产线全局状态机（`LINE`：真实产线，`VIRTUAL`：虚拟仿真产线）。<br>• ST01~ST06 工位节拍推进、托盘移载判定、机械臂动作与良品/不良品判定。<br>• 常驻后台 `line_simulator` 仿真推进守护线程（支持 1x/2x/5x/10x 动态调速）。<br>• 工单完工自动收尾、过站质量履历记录与 PLC 寄存器反向联动控制。 |

### 3. 业务 API 蓝图 (`backend/routes/`)
| 文件名 | 蓝图名称 | 功能说明 |
| :--- | :--- | :--- |
| [backend/routes/auth.py](file:///f:/s/Server/backend/routes/auth.py) | `auth_bp` | **认证与用户会话**：`/api/me`, `/api/login`, `/api/logout`, `/api/captcha`（Pillow 验证码生成）, `/api/register`, `/api/forgot_password`, `/api/change_password`。 |
| [backend/routes/orders.py](file:///f:/s/Server/backend/routes/orders.py) | `orders_bp` | **工单排产调度**：`/api/orders`（查询/创建/删除）, `/api/orders/<id>/dispatch`（下发投产）, `/api/orders/<id>/progress`（手动调进度/返工）, `/api/demo/seed_orders`（生成示范工单）。 |
| [backend/routes/line.py](file:///f:/s/Server/backend/routes/line.py) | `line_bp` | **产线状态与控制**：`/api/line`, `/api/virtual`, `/api/line/command`（启动/停止/复位/急停）, `/api/simulation/speed`（倍速调节）, `/api/line/station/<code>/diagnose`（工位深度诊断）, `/api/line/inject_fault`（模拟故障注入）, `/api/line/clear_fault`（故障复位）。 |
| [backend/routes/scada.py](file:///f:/s/Server/backend/routes/scada.py) | `scada_bp` | **设备监控与 SCADA**：`/api/plc_status`（PLC 寄存器与产线联动状态）, `/api/station_st01`（ST01 上料工位气缸/传感器专控）, `/api/plc/write`（手动写寄存器）, `/api/plc/config`（修改 PLC 地址并重连）。 |
| [backend/routes/production.py](file:///f:/s/Server/backend/routes/production.py) | `production_bp` | **品质追溯与统计导出**：`/api/production`（质量趋势与工位统计）, `/api/stats`（大屏总览指标）, `/api/export/orders`（导出工单 CSV）, `/api/export/trace`（导出质量追溯链 CSV）。 |
| [backend/routes/system.py](file:///f:/s/Server/backend/routes/system.py) | `system_bp` | **系统管理与配置**：`/api/config`（静态配置数据）, `/api/ping`（会话心跳探活）, `/api/logs`（生产操作日志）, `/api/users`（用户列表/角色变更/启停/重置密码）。 |

---

## 四、前端入口与页面 (`frontend/` & `frontend/pages/`)

### 1. 单页宿主入口
| 文件名 | 功能说明 |
| :--- | :--- |
| [frontend/index.html](file:///f:/s/Server/frontend/index.html) | **SPA 核心外壳与入口**。<br>• 包含工业级深色主题全局布局、响应式侧边导航栏（Sidebar）、顶部状态栏（Topbar）。<br>• 包含全屏页面容器（登录/注册/找回密码）、主工作区（Workspace）、工位诊断抽屉（Drawer）与全局 Toast 提示容器。<br>• 按依赖顺序统一加载全局 CSS 样式与 JS 交互脚本。 |

### 2. 页面模板片段 (`frontend/pages/`)
*所有页面均为纯 HTML 片段，由前端路由器 `router.js` 动态加载到主窗口，不含任何服务端模板标签。*

| 文件名 | 对应路由 | 功能说明 |
| :--- | :--- | :--- |
| [frontend/pages/login.html](file:///f:/s/Server/frontend/pages/login.html) | `#/login` | **用户登录页面**：支持账号、密码输入、图形验证码验证（支持一键点击刷新）、记住我选项。 |
| [frontend/pages/register.html](file:///f:/s/Server/frontend/pages/register.html) | `#/register` | **新用户注册页面**：支持用户名、密码规则校验、确认密码与验证码提交。 |
| [frontend/pages/forgot_password.html](file:///f:/s/Server/frontend/pages/forgot_password.html) | `#/forgot_password` | **密码重置页面**：支持输入用户名与验证码重置登录密码。 |
| [frontend/pages/home.html](file:///f:/s/Server/frontend/pages/home.html) | `#/home` | **数据采集与实时看板（首页）**：展示全线 OEE、产线实时节拍、当日良品率、各工位生产负荷图表与产线动态概览。 |
| [frontend/pages/line.html](file:///f:/s/Server/frontend/pages/line.html) | `#/line` | **在制实时追踪页面**：可视化展示 ST01～ST06 各工位物理托盘在制位置、工装板状态与流水线实时流动。 |
| [frontend/pages/virtual.html](file:///f:/s/Server/frontend/pages/virtual.html) | `#/virtual` | **虚拟仿真产线页面**：脱机演示模式，动态模拟托盘流转、装配动作、测试波形与合格检测，无需物理硬件支持。 |
| [frontend/pages/orders.html](file:///f:/s/Server/frontend/pages/orders.html) | `#/orders` | **作业指示排产页面**：生产工单的创建、编辑、下发排产、状态推进（待排产/生产中/已完成）与批次统计。 |
| [frontend/pages/production.html](file:///f:/s/Server/frontend/pages/production.html) | `#/production` | **品质追溯分析页面**：支持按产品序列号、工单号查询历史装配记录、工位测试数据、质量合格状态与质检报告。 |
| [frontend/pages/recipes.html](file:///f:/s/Server/frontend/pages/recipes.html) | `#/recipes` | **工艺配方节拍配置页面**：管理各产品型号对应的各工位装配工艺参数、目标标准节拍与公差范围。 |
| [frontend/pages/station_st01.html](file:///f:/s/Server/frontend/pages/station_st01.html) | `#/station_st01` | **ST01 自动上料工位专控（SCADA）**：实时监测上料气缸、料仓光电传感器、上料步进电机状态，支持手动寸动调试与复位。 |
| [frontend/pages/hmi.html](file:///f:/s/Server/frontend/pages/hmi.html) | `#/hmi` | **设备上位机主控台**：提供产线总启停、急停联动、自动/手动模式切换、PLC Modbus 寄存器在线点位监控。 |
| [frontend/pages/alarms.html](file:///f:/s/Server/frontend/pages/alarms.html) | `#/alarms` | **异常发生与报警管理页面**：实时展示产线告警事件、按等级（致命/严重/警告）筛选、支持报警确认与清除。 |
| [frontend/pages/settings.html](file:///f:/s/Server/frontend/pages/settings.html) | `#/settings` | **基础数据与系统设置页面**：修改 PLC 通讯 IP/端口、数据库连接串、产线模拟步长与运行参数（管理员专属）。 |
| [frontend/pages/users.html](file:///f:/s/Server/frontend/pages/users.html) | `#/users` | **用户权限管理页面**：管理系统账号、修改用户角色（管理员/操作工）、重置用户密码、启停账号（管理员专属）。 |

---

## 五、前端样式表 (`frontend/static/css/`)

| 文件名 | 功能说明 |
| :--- | :--- |
| [frontend/static/css/app.css](file:///f:/s/Server/frontend/static/css/app.css) | **系统全局基础样式库**。<br>• 定义工业暗色主题设计规范（CSS 变量、调色板、圆角、阴影）。<br>• 定义侧边栏、顶部栏、主要工作区、按钮组、表单控件、弹窗（Modal）、侧边抽屉（Drawer）等通用样式。 |
| [frontend/static/css/console.css](file:///f:/s/Server/frontend/static/css/console.css) | **工业 HUD 命令中心视效增强样式**。<br>• 仪表盘光晕、工业级面板边框、状态指示灯流光、霓虹数据高亮与数字化大屏视效。 |
| [frontend/static/css/base.css](file:///f:/s/Server/frontend/static/css/base.css) | **通用排版与原子样式工具类**（间距、对齐、文字截断、Flex 布局辅助等）。 |
| [frontend/static/css/monitor.css](file:///f:/s/Server/frontend/static/css/monitor.css) | **大屏监控与卡片组件样式**。<br>• 工位方块、指标卡片（KPI）、图表容器与实时状态小部件样式。 |
| [frontend/static/css/home.css](file:///f:/s/Server/frontend/static/css/home.css) | **看板首页专用样式**。<br>• OEE 环形指示、产量柱状图、工位节拍对比等网格布局样式。 |
| [frontend/static/css/line.css](file:///f:/s/Server/frontend/static/css/line.css) | **产线在制追踪样式**。<br>• 传送带轨道、工位节点、托盘流向箭头、在制品状态徽标样式。 |
| [frontend/static/css/orders.css](file:///f:/s/Server/frontend/static/css/orders.css) | **工单排产页面样式**。<br>• 数据表格、工单状态标签（进行中/已完成/异常）、排产操作弹窗样式。 |
| [frontend/static/css/production.css](file:///f:/s/Server/frontend/static/css/production.css) | **品质追溯页面样式**。<br>• 批次搜索栏、时间轴履历卡片、检验结果判定卡片样式。 |
| [frontend/static/css/recipes.css](file:///f:/s/Server/frontend/static/css/recipes.css) | **工艺配方页面样式**。<br>• 配方卡片网格、工位节拍参数配置表单样式。 |
| [frontend/static/css/station_st01.css](file:///f:/s/Server/frontend/static/css/station_st01.css) | **ST01 上料工位专用 SCADA 样式**。<br>• 模拟气缸伸缩状态指示、传感器点位图示、寸动控制按钮排布。 |
| [frontend/static/css/hmi.css](file:///f:/s/Server/frontend/static/css/hmi.css) | **设备上位机控制台样式**。<br>• 大号工业启动/急停旋钮、手自动切换拨钮、PLC 寄存器网格监视卡片样式。 |
| [frontend/static/css/alarms.css](file:///f:/s/Server/frontend/static/css/alarms.css) | **报警管理页面样式**。<br>• 告警等级红黄橙色标、呼吸闪烁动画、异常确认按钮样式。 |
| [frontend/static/css/settings.css](file:///f:/s/Server/frontend/static/css/settings.css) | **系统设置页面样式**。<br>• 配置分组面板、表单行栅格、网络连通性测试指示器样式。 |
| [frontend/static/css/users.css](file:///f:/s/Server/frontend/static/css/users.css) | **用户管理页面样式**。<br>• 用户列表表格、权限徽章、添加用户与修改密码弹窗样式。 |
| [frontend/static/css/login.css](file:///f:/s/Server/frontend/static/css/login.css) | **登录页面专属样式**。<br>• 居中磨砂玻璃登录卡片、科技风背景网格、验证码输入行。 |
| [frontend/static/css/register.css](file:///f:/s/Server/frontend/static/css/register.css) | **注册页面专属样式**。<br>• 注册表单卡片、密码强度提示、提交按钮交互效果。 |
| [frontend/static/css/forgot_password.css](file:///f:/s/Server/frontend/static/css/forgot_password.css) | **找回密码页面专属样式**。<br>• 密码重置引导卡片、验证码与新密码输入行样式。 |

---

## 六、前端交互脚本 (`frontend/static/js/`)

### 1. 核心底层框架模块
| 文件名 | 功能说明 |
| :--- | :--- |
| [frontend/static/js/api.js](file:///f:/s/Server/frontend/static/js/api.js) | **统一 API 通讯客户端**。<br>• 封装 `fetch` 请求，自动携带 Cookie 凭证（`credentials: 'include'`）。<br>• 统一处理网络错误、JSON 解析、Toast 错误提示及 401 未登录自动跳转。 |
| [frontend/static/js/auth.js](file:///f:/s/Server/frontend/static/js/auth.js) | **认证与用户会话管理**。<br>• 管理当前登录用户状态（`currentUser` 缓存）。<br>• 提供 `login()`、`logout()`、`register()`、`checkAuth()` 及角色权限校验函数。 |
| [frontend/static/js/router.js](file:///f:/s/Server/frontend/static/js/router.js) | **客户端 SPA 路由器**。<br>• 监听 `hashchange` 事件（如 `#/home`、`#/orders`）。<br>• 维护路由白名单与权限守卫（未登录重定向至登录页，非管理员阻断敏感页面）。<br>• 异步按需拉取 `pages/*.html` 片段并插入工作区，自动调用对应页面的初始化钩子。 |
| [frontend/static/js/app.js](file:///f:/s/Server/frontend/static/js/app.js) | **全局应用层逻辑**。<br>• 侧边栏折叠/展开、当前页面高亮切换。<br>• 全局弹窗（Modal）开关逻辑。<br>• 右侧工位诊断抽屉（Drawer）数据渲染与打开/关闭。<br>• 全局 Toast 消息提示组件（`showToast(msg, type)`）。 |
| [frontend/static/js/base.js](file:///f:/s/Server/frontend/static/js/base.js) | **公共工具函数库**。<br>• 日期与时间格式化、百分比格式化、数值千分位、防抖/节流函数。 |
| [frontend/static/js/form.js](file:///f:/s/Server/frontend/static/js/form.js) | **表单验证与辅助工具**。<br>• 表单序列化为 JSON 对象、字段必填与正则格式校验。 |

### 2. 页面业务逻辑脚本
| 文件名 | 功能说明 |
| :--- | :--- |
| [frontend/static/js/login.js](file:///f:/s/Server/frontend/static/js/login.js) | **登录业务逻辑**：绑定登录表单提交、验证码图片点击刷新、记住密码逻辑、登录成功跳转。 |
| [frontend/static/js/register.js](file:///f:/s/Server/frontend/static/js/register.js) | **注册业务逻辑**：注册表单前后端校验、密码两次一致性检查、注册成功后自动引导登录。 |
| [frontend/static/js/forgot_password.js](file:///f:/s/Server/frontend/static/js/forgot_password.js) | **找回密码逻辑**：验证码校验与新密码提交重置。 |
| [frontend/static/js/home.js](file:///f:/s/Server/frontend/static/js/home.js) | **看板业务逻辑**：定时轮询 `/api/home_stats`，动态刷新看板 KPI、OEE 环形图与生产趋势图表。 |
| [frontend/static/js/line.js](file:///f:/s/Server/frontend/static/js/line.js) | **产线在制业务逻辑**：定时轮询 `/api/line_status`，动态渲染 ST01～ST06 工位在制状态与托盘位移。 |
| [frontend/static/js/orders.js](file:///f:/s/Server/frontend/static/js/orders.js) | **工单排产业务逻辑**：获取工单列表、分页筛选、创建新工单、修改工单状态（下发投产/暂停/完成）。 |
| [frontend/static/js/production.js](file:///f:/s/Server/frontend/static/js/production.js) | **品质追溯业务逻辑**：按批次/序列号查询历史装配信息，渲染质量检测参数与质检合格分析。 |
| [frontend/static/js/recipes.js](file:///f:/s/Server/frontend/static/js/recipes.js) | **工艺配方业务逻辑**：加载配方列表、新增/编辑工艺配方、下发各工位标准节拍到后台。 |
| [frontend/static/js/station_st01.js](file:///f:/s/Server/frontend/static/js/station_st01.js) | **ST01 工位控制逻辑**：定时读取 ST01 传感器/气缸寄存器，处理寸动/单步/复位等控制指令。 |
| [frontend/static/js/hmi.js](file:///f:/s/Server/frontend/static/js/hmi.js) | **上位机主控逻辑**：产线一键启动/急停控制、手自动模式切换、PLC 寄存器点位监视与调试。 |
| [frontend/static/js/alarms.js](file:///f:/s/Server/frontend/static/js/alarms.js) | **报警管理业务逻辑**：定时拉取告警列表、告警静音/确认、异常记录清除。 |
| [frontend/static/js/settings.js](file:///f:/s/Server/frontend/static/js/settings.js) | **系统配置业务逻辑**：读取当前系统配置、保存 PLC 通讯参数与数据库参数并触发重连。 |
| [frontend/static/js/users.js](file:///f:/s/Server/frontend/static/js/users.js) | **用户管理业务逻辑**：用户列表渲染、新建账号、修改角色权限（admin/operator）、重置指定用户密码。 |
| [frontend/static/js/progress.js](file:///f:/s/Server/frontend/static/js/progress.js) | **进度条组件逻辑**：通用的工单生产进度条渲染与动态百分比计算。 |

---

## 七、配置文件与数据目录 (`config/` & `data/`)

| 文件名 | 功能说明 |
| :--- | :--- |
| [config/config.json](file:///f:/s/Server/config/config.json) | **系统全局配置文件**。<br>• 包含服务监听端口（`frontend_port: 6021`, `backend_port: 8080`）。<br>• 数据库连接参数（MySQL 地址/端口/用户/密码/库名）。<br>• PLC Modbus TCP 目标设备 IP 与端口（默认 `127.0.0.1:502`）。 |
| [data/mes.db](file:///f:/s/Server/data/mes.db) | **本地 SQLite 数据库文件**。<br>• 当远程 MySQL 无法连接时，系统自动切换至此文件，保证用户管理、工单、配方、报警与日志功能完全可用。 |
| `data/.epoch` | **会话世代标识文件**。<br>• 记录服务启动时的 Session Epoch 时间戳，用于统一校验浏览器登录 Cookie 是否过期失效。 |
| `data/.last_db` | **数据库类型记录文件**。<br>• 记录系统最近一次成功连接的数据库类型（`mysql` 或 `sqlite`）。 |
| `data/sync/` | **数据同步临时目录**。<br>• 预留用于前后端数据同步、离线数据包暂存的目录。 |

---

## 八、自动化测试与文档目录

### 1. 自动化测试 (`tests/`)
| 文件名 | 功能说明 |
| :--- | :--- |
| [tests/check_ui.py](file:///f:/s/Server/tests/check_ui.py) | **系统全链路自检脚本**。<br>• 自动化测试静态服务（6021）和 API 接口服务（8080）是否正常在线。<br>• 校验所有静态资源文件、页面模板以及关键 API 接口的 HTTP 响应状态。 |

### 2. 源码深度解析文档 (`代码讲解/`)
| 文件名 | 功能说明 |
| :--- | :--- |
| [代码讲解/00-讲解总览与系统架构.md](file:///f:/s/Server/代码讲解/00-讲解总览与系统架构.md) | 系统总体架构设计、技术选型与模块分工总览。 |
| [代码讲解/10-后端01-common公共模块讲解.md](file:///f:/s/Server/代码讲解/10-后端01-common公共模块讲解.md) | 深度剖析 `common.py` 的双模数据库设计、安全认证与通用方法。 |
| [代码讲解/11-后端02-server页面服务讲解.md](file:///f:/s/Server/代码讲解/11-后端02-server页面服务讲解.md) | 静态文件服务与 SPA 路由机制的演进历程与实现细节。 |
| [代码讲解/12-后端03-api_server接口服务讲解.md](file:///f:/s/Server/代码讲解/12-后端03-api_server接口服务讲解.md) | 后端 8080 端口全部 RESTful API 接口与业务逻辑逐条解析。 |
| [代码讲解/13-后端04-虚拟PLC与辅助脚本讲解.md](file:///f:/s/Server/代码讲解/13-后端04-虚拟PLC与辅助脚本讲解.md) | Modbus TCP 虚拟 PLC 模拟器、寄存器映射与仿真引擎详解。 |
| [代码讲解/20-前端01-HTML模板逐页讲解.md](file:///f:/s/Server/代码讲解/20-前端01-HTML模板逐页讲解.md) | 14 个页面 HTML 结构与 DOM 设计详细解析。 |
| [代码讲解/21-前端02-CSS样式文件讲解.md](file:///f:/s/Server/代码讲解/21-前端02-CSS样式文件讲解.md) | 17 个 CSS 样式文件的样式规范、色彩体系与响应式布局解析。 |
| [代码讲解/22-前端03-JS脚本逐文件讲解.md](file:///f:/s/Server/代码讲解/22-前端03-JS脚本逐文件讲解.md) | 20 个 JS 文件的架构设计、接口调用与交互实现逐行剖析。 |
| [代码讲解/30-附录-前后端对应关系总表.md](file:///f:/s/Server/代码讲解/30-附录-前后端对应关系总表.md) | 页面、HTML 模板、CSS 样式、JS 脚本与后端 API 接口的完整映射对应表。 |
