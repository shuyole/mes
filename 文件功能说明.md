# 全量文件功能对应说明

本文档对本项目中的每一个文件及其所承担的功能进行详细分类与说明，便于快速定位与学习二次开发。

---

## 一、根目录文件

| 文件路径 | 文件类型 | 功能说明 |
| :--- | :--- | :--- |
| `start.bat` | Windows 批处理脚本 | **系统一键启动入口**。按顺序启动 6070 数据库服务、8090 后端接口服务、6031 前端静态服务，并自动打开浏览器访问登录页面。 |
| `requirements.txt` | 依赖配置文件 | **Python 依赖包清单**。包含 Flask、Flask-CORS、requests、PyMySQL、Pillow 等必要依赖。 |
| `README.md` | Markdown 说明 | **项目快速自述**。包含系统特性、快速启动说明与目录概览。 |
| `使用指南.md` | Markdown 说明 | **系统使用手册**。包含运行环境、测试账号、功能操作步骤与常见排错指南。 |
| `前后端分离与模块化架构说明.md` | Markdown 说明 | **架构解析文档**。深入剖析三服务分工、零 Session 机制以及原生 SQLite/MySQL 独立设计。 |
| `文件功能说明.md` | Markdown 说明 | **本文件**。全项目所有源码文件的作用对应总览表。 |
| `需要的应用.txt` | 文本说明 | **软件环境清单**。列出运行本项目所需的操作系统、Python 版本及浏览器要求。 |
| `.gitignore` | Git 忽略规则 | **版本控制忽略文件**。配置忽略本地运行时生成的临时数据（如 `data/`、`config.json` 等）。 |

---

## 二、后端服务模块 (`backend/`)

| 文件路径 | 代码行数 | 功能说明 |
| :--- | :--- | :--- |
| `backend/static_server.py` | 约 30 行 | **前端静态托管服务（端口 6031）**。<br>• 纯静态文件托管，不连接数据库，不包含任何业务接口。<br>• 将 `/login`、`/register`、`/home` 等前端路由映射到对应的 HTML 页面模板。 |
| `backend/api_server.py` | 约 35 行 | **后端接口主入口服务（端口 8090）**。<br>• 纯 RESTful JSON API 服务，开启跨域支持。<br>• 挂载路由蓝图（`routes/auth.py`）。<br>• 自身不直接连接数据库，所有数据操作交由 6070 服务执行。 |
| `backend/db_server.py` | 约 65 行 | **数据库统一代理服务（端口 6070）**。<br>• 全系统唯一直接连接数据库底层的进程。<br>• 根据 `config.json` 配置自动引入 `db_sqlite` 或 `db_mysql`。<br>• 对外提供 `/query`（查询）与 `/execute`（写入）HTTP 接口。 |
| `backend/db_sqlite.py` | 约 70 行 | **原生 SQLite 数据库实现模块**。<br>• 基于 Python 标准库 `sqlite3` 实现。<br>• 严格使用 SQLite 原生标准的问号占位符 `?`。<br>• 负责在 `data/auth.db` 中自动建表并写入初始默认账号。 |
| `backend/db_mysql.py` | 约 80 行 | **原生 MySQL 数据库实现模块**。<br>• 基于 `pymysql` 库实现。<br>• 严格使用 MySQL 原生标准的格式化占位符 `%s`。<br>• 自动创建 `auth_db` 数据库及对应的数据表，写入初始默认账号。 |
| `backend/common.py` | 约 50 行 | **后端公共辅助模块**。<br>• 读取并初始化 `config.json` 配置文件。<br>• 提供 `db_query()` 与 `db_execute()` 工具函数，通过 HTTP POST 方式调用 6070 数据库服务执行 SQL。 |
| `backend/routes/__init__.py` | 约 10 行 | **路由包初始化文件**。<br>• 集中导出路由蓝图对象。 |
| `backend/routes/auth.py` | 约 180 行 | **核心认证业务蓝图**。<br>• `/api/captcha`：使用 Pillow 库实时生成彩色图形验证码图片。<br>• `/api/login`：账号密码核对，验证通过后直接返回用户信息。<br>• `/api/register`：新账号注册，检测用户名与学号唯一性。<br>• `/api/forgot_password`：通过核对用户名与学号，直接重置新密码。<br>• `/api/change_password`：用户在个人中心输入旧密码与新密码进行修改。 |

---

## 三、前端页面模板 (`frontend/Templates/`)

| 文件路径 | 渲染方式 | 功能说明 |
| :--- | :--- | :--- |
| `frontend/Templates/login.html` | 原生静态 HTML | **用户登录页面**。<br>包含用户名、密码输入框、图形验证码点击刷新区域、记住密码复选框、注册入口与忘记密码入口。 |
| `frontend/Templates/register.html` | 原生静态 HTML | **新用户注册页面**。<br>包含用户名、真实姓名、学号/工号（用于后续找回密码）、密码、确认密码及验证码表单。 |
| `frontend/Templates/forgot_password.html` | 原生静态 HTML | **找回密码页面**。<br>通过填写用户名、绑定的学号/工号进行身份安全核验，成功后直接设定新密码。 |
| `frontend/Templates/home.html` | 原生静态 HTML | **系统控制台主页面**。<br>展示登录成功后的欢迎看板、系统概览卡片、用户信息展示面板及快捷操作入口。 |
| `frontend/Templates/settings.html` | 原生静态 HTML | **个人设置与管理页面**。<br>展示个人详细信息、账户权限说明，并提供密码修改弹窗交互。 |

---

## 四、前端静态样式与交互脚本 (`frontend/static/`)

### 1. 样式表 (`frontend/static/css/`)
| 文件路径 | 作用页面 | 功能说明 |
| :--- | :--- | :--- |
| `base.css` | 全局基础 | 重置浏览器默认样式、定义基础排版与现代无衬线字体。 |
| `login.css` | `login.html` | 现代毛玻璃登录卡片视觉、输入框动效、粒子容器布局与响应式适配。 |
| `register.css` | `register.html` | 注册卡片居中布局、字段排版及错误提示动效。 |
| `forgot_password.css` | `forgot_password.html` | 找回密码流程样式、提交按钮及返回登录链接设计。 |
| `shell.css` | `home.html` / `settings.html` | 登录后主控制台的通用导航顶栏、左侧菜单栏、卡片容器与修改密码弹窗样式。 |

### 2. 交互脚本 (`frontend/static/js/`)
| 文件路径 | 依赖关系 | 功能说明 |
| :--- | :--- | :--- |
| `api.js` | 底层网络依赖 | **统一网络请求工具**。<br>封装原生的 `fetch` 调用，统一指向 `http://127.0.0.1:8090`，自动处理 JSON 序列化与异常捕获。 |
| `login.js` | 依赖 `api.js` | **登录业务交互**。<br>绑定表单提交事件，调用 `/api/login` 接口；登录成功后将用户信息写入 `localStorage` 并跳转主页。 |
| `register.js` | 依赖 `api.js` | **注册业务交互**。<br>前端校验密码长度与两次输入一致性，调用 `/api/register` 接口完成注册并引导登录。 |
| `forgot_password.js` | 依赖 `api.js` | **找回密码交互**。<br>采集用户名、学号与新密码，调用 `/api/forgot_password` 重置密码。 |
| `shell.js` | 依赖 `api.js` | **控制台通用鉴权与外壳脚本**。<br>进入页面时检查 `localStorage.getItem("user")`，未登录强制重定向回登录页；提供修改密码弹窗与退出登录功能。 |
| `settings.js` | 依赖 `api.js` | **设置页专有交互**。<br>渲染当前登录人的详细档案与权限信息。 |
| `particles.js` | 独立运行 | **纯原生 Canvas 背景粒子特效**。<br>为登录、注册、找回密码页提供轻量美观的动态粒子交互连线背景。 |

---

## 五、系统配置目录 (`config/`)

| 文件路径 | 功能说明 |
| :--- | :--- |
| `config/config.example.json` | **配置模板文件**。包含默认的端口设置、SQLite 与 MySQL 示例参数。在代码仓库中持久保存。 |
| `config/config.json` | **系统运行时实际配置文件**。系统首次启动时自动从模板生成，本地修改数据库密码或端口均在此文件中配置。已配置在 `.gitignore` 中，防止敏感账号密码被提交。 |
